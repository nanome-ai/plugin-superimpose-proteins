version: '3'
services:
  redis:
    image: redis
  
  plugin_service:
    image: plugin-cookbook_plugin_service
    tty: true
    environment:
      - DEFAULT_URL=http://127.0.0.1:8888
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - REDIS_PASSWORD=${REDIS_PASSWORD:-}
      - NTS_HOST=${NTS_HOST:-}
      - NTS_PORT=${NTS_PORT:-}
      - NTS_KEY=${NTS_KEY:-}
      - ARGS=${ARGS:-}
      - JUPYTER_TOKEN=${JUPYTER_TOKEN:-change_me}
      - REDIS_CHANNEL=${REDIS_CHANNEL:-}
      - PLUGIN_NAME=[cb]-superimpose
    depends_on:
      - redis

  superposition_cookbook:
    build:
      dockerfile: docker/Dockerfile
      target: cookbook
      context: ../
    working_dir: /app/cookbook/
    environment:
      - REDIS_HOST=${REDIS_HOST:-redis}
      - REDIS_PORT=${REDIS_PORT:-6379}
      - JUPYTER_ENABLE_LAB=yes
      - JUPYTER_TOKEN=${JUPYTER_TOKEN:-change_me}
      - REDIS_CHANNEL=${REDIS_CHANNEL:-}
    ports:
      - 8888:8888
    volumes:
      - /home/mike/workspace/rmsd-2/plugin:/app/plugin
      - /home/mike/workspace/rmsd-2/cookbook/notebooks:/app/cookbook/notebooks
      - /home/mike/workspace/rmsd-2/cookbook/temp:/tmp

    tty: true
    stdin_open: true
    depends_on:
      - redis
    command: jupyter lab --ip 0.0.0.0 --allow-root
